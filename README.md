# STREAMLIT-WORKBOOK
This Streamlit app enables real-time webcam streaming using OpenCV. When the user checks the "Start Webcam" checkbox, the app captures video frames from the webcam and displays them in the Streamlit interface. The video feed is processed in RGB format to ensure proper color display and is shown in a placeholder window.
